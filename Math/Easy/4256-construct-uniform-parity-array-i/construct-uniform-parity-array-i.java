class Solution {
    /**
     * Approach : Using Math Approach
     *
     * TC : O(1)
     * SC : O(1)
     */
    public boolean uniformArray(int[] nums1) {
        /**
         * Mathematically, it is always possible 
         * to make the array uniform
         */
        return true;
    }
}
